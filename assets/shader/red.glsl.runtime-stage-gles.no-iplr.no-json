#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct TheBlock
{
    float theMember;
};

struct main0_out
{
    float4 fragColor [[color(0)]];
};

fragment main0_out main0(constant TheBlock& _14 [[buffer(0)]])
{
    main0_out out = {};
    out.fragColor = float4(1.0, cos(_14.theMember), 0.0, 1.0);
    return out;
}

