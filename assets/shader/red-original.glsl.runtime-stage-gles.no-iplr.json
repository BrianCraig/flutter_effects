#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 fragColor [[color(0)]];
};

fragment main0_out main0(constant float& iTime [[buffer(0)]])
{
    main0_out out = {};
    out.fragColor = float4(1.0, cos(iTime), 0.0, 1.0);
    return out;
}

